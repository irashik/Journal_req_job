/*
 * тестирование файла
 * Что будет происходить при успешном старте
 * проверяем загрузку модулей
 * 
 * что будет при ошибках?
 * проверяем мидлевары
 * 
 * проверяем обработчики ошибок
 * 
 */

const assert = require("assert");
const app = require('./app');

describe("App.js testing", function() {
    it ('проверяем загрузку всех модулей', function() {
        
    });
    
    it("проверяем загрузку middleware", function() {
        
    });
});